# Pyla Note

A simple note-taking app !

# Installation

Use the following command:

`npm i`

# Configuration

Go see the .env and follow the instructions.

---

You can now use the command `npm run dev` and everything is good!
